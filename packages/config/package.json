{
  "name": "@ecommerce/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/env.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": { "zod": "catalog:" },
  "devDependencies": { "@types/node": "latest", "typescript": "catalog:", "vitest": "catalog:" }
}
